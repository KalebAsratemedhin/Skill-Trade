from django.contrib import admin
from .models import TechnicianProfile, User

admin.site.register(User)
admin.site.register(TechnicianProfile)  # Registers the User model for admin
  # Registers the User model for admin
